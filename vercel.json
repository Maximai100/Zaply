{
  "version": 2,
  "buildCommand": "cd apps/frontend && npm ci --no-audit --no-fund && npm run vercel-build",
  "outputDirectory": "apps/frontend/dist",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}


