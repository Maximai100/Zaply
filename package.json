{
  "name": "zaply",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "cd apps/frontend && VITE_API_URL=https://1.cycloscope.online npm run build",
    "dev": "cd apps/frontend && npm run dev"
  }
}
